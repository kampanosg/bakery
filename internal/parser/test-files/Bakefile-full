version: v1
metadata:
    author: "Darth Vader"
variables:
    path: "/home/darth-vader"
defaults:
    - list
recipes:
    create-user:
        description: "creates the user dir"
        private: false
        steps:
            - "ls -al"
            - "mkdir :path:"
