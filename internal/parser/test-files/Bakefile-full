version: v1
metadata:
    author: "Golan Trevize"
variables:
    name: test
defaults:
    - list
recipes:
    list:
        description: "list the current directory"
        steps:
            - "ls -al"
            - "mkdir :name:"
